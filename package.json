{
  "name": "aws-ssm-remote-port-forwarding",
  "version": "0.0.0",
  "private": true,
  "description": "Forward a remote port to a local port using AWS SSM",
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "homepage": "https://github.com/enkhjile/aws-ssm-remote-port-forwarding",
  "bugs": {
    "url": "https://github.com/enkhjile/aws-ssm-remote-port-forwarding/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/enkhjile/aws-ssm-remote-port-forwarding.git"
  },
  "license": "MIT",
  "author": "Enkhjil Enkhbaatar <enkhjil.eb@gmail.com>",
  "scripts": {
    "all": "npm run test && npm run coverage && npm run package",
    "bundle": "npx biome check && npm run package",
    "coverage": "npx make-coverage-badge --output-path ./badges/coverage.svg",
    "package": "npm run package:main && npm run package:post",
    "package:main": "npx ncc build src/main.ts -o dist/main --license licenses.txt && cp src/connect-with-port-forwarding.sh dist/main/connect-with-port-forwarding.sh",
    "package:post": "npx ncc build src/post.ts -o dist/post --license licenses.txt",
    "package:watch": "npm run package -- --watch",
    "test": "npx jest"
  },
  "jest": {
    "clearMocks": true,
    "collectCoverage": true,
    "collectCoverageFrom": [
      "./src/**"
    ],
    "coverageReporters": [
      "json-summary",
      "text",
      "lcov"
    ],
    "moduleFileExtensions": [
      "js",
      "ts"
    ],
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/*.test.ts"
    ],
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ],
    "transform": {
      "^.+\\.ts$": "ts-jest"
    },
    "verbose": true
  },
  "dependencies": {
    "@actions/core": "^2.0.1",
    "@aws-sdk/client-ssm": "^3.966.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.11.24",
    "@vercel/ncc": "^0.38.4",
    "jest": "^29.7.0",
    "lefthook": "^2.0.12",
    "make-coverage-badge": "^1.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
